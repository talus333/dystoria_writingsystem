{
  "name": "dystoria-writingsystem",
  "version": "1.0.0",
  "private": true,
  "description": "Dystoria writing app — test harness for the single-file index.html.",
  "scripts": {
    "test": "node tests/sections.test.js && node tests/helpers.test.js && node tests/smoke.test.js"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
